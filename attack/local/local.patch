diff --git a/src/app/homePage/page.tsx b/src/app/homePage/page.tsx
index 9540026..3f69963 100644
--- a/src/app/homePage/page.tsx
+++ b/src/app/homePage/page.tsx
@@ -107,6 +107,7 @@ export default function HomePage() {
 		return;
 	}
 
+
 	return (
 		<>
 			<AuthenticatedLayout title="SecureDove Messaging App">
diff --git a/src/lib/custom-session-claims.ts b/src/lib/custom-session-claims.ts
index f91aead..a6c28ae 100644
--- a/src/lib/custom-session-claims.ts
+++ b/src/lib/custom-session-claims.ts
@@ -4,13 +4,14 @@ import { getFirebaseAuth } from "next-firebase-auth-edge";
 
 export async function createTwoFactorVerifiedToken(userId: string): Promise<string> {
 	try {
-		const SERVICE_ACCOUNT = process.env["SERVICE_ACCOUNT"];
+		// const SERVICE_ACCOUNT = process.env["SERVICE_ACCOUNT"];
 
-		if (!SERVICE_ACCOUNT) {
-			throw new Error("Missing SERVICE_ACCOUNT environment variable");
-		}
+		// if (!SERVICE_ACCOUNT) {
+		// 	throw new Error("Missing SERVICE_ACCOUNT environment variable");
+		// }
 
-		const serviceAccount = JSON.parse(SERVICE_ACCOUNT);
+		// const serviceAccount = JSON.parse(SERVICE_ACCOUNT);
+		const serviceAccount = require("/root/webapp/firebase-admin.json");
 		const auth = getFirebaseAuth({
 			serviceAccount: {
 				projectId: serviceAccount.project_id,
@@ -26,7 +27,7 @@ export async function createTwoFactorVerifiedToken(userId: string): Promise<stri
 		});
 
 		console.log(`[Custom Claims] Created 2FA verified custom token for user: ${userId}`);
-		return customToken;
+        return customToken;
 	} catch (error) {
 		console.error("Error creating 2FA custom token:", error);
 		throw error;
diff --git a/src/lib/email.ts b/src/lib/email.ts
index ff7b480..b0c3297 100644
--- a/src/lib/email.ts
+++ b/src/lib/email.ts
@@ -2,40 +2,41 @@
  * Send email using Resend API directly (Edge Runtime compatible)
  */
 async function sendEmailViaResendAPI(to: string, subject: string, html: string, text: string): Promise<{ success: boolean; error?: string; id?: string }> {
-	const apiKey = process.env.RESEND_API_KEY;
+	// const apiKey = process.env.RESEND_API_KEY;
 
-	if (!apiKey) {
-		return { success: false, error: "Resend API key not configured" };
-	}
+	// if (!apiKey) {
+	// 	return { success: false, error: "Resend API key not configured" };
+	// }
 
-	const fromEmail = process.env.RESEND_FROM_EMAIL || "onboarding@resend.dev";
+	// const fromEmail = process.env.RESEND_FROM_EMAIL || "onboarding@resend.dev";
 
-	try {
-		const response = await fetch("https://api.resend.com/emails", {
-			method: "POST",
-			headers: {
-				Authorization: `Bearer ${apiKey}`,
-				"Content-Type": "application/json",
-			},
-			body: JSON.stringify({
-				from: fromEmail,
-				to: [to],
-				subject,
-				html,
-				text,
-			}),
-		});
+	// try {
+	// 	const response = await fetch("https://api.resend.com/emails", {
+	// 		method: "POST",
+	// 		headers: {
+	// 			Authorization: `Bearer ${apiKey}`,
+	// 			"Content-Type": "application/json",
+	// 		},
+	// 		body: JSON.stringify({
+	// 			from: fromEmail,
+	// 			to: [to],
+	// 			subject,
+	// 			html,
+	// 			text,
+	// 		}),
+	// 	});
 
-		const data = await response.json();
+	// 	const data = await response.json();
 
-		if (!response.ok) {
-			return { success: false, error: data.message || "Failed to send email" };
-		}
+	// 	if (!response.ok) {
+	// 		return { success: false, error: data.message || "Failed to send email" };
+	// 	}
 
-		return { success: true, id: data.id };
-	} catch (error) {
-		return { success: false, error: error instanceof Error ? error.message : "Unknown error" };
-	}
+	// 	return { success: true, id: data.id };
+	// } catch (error) {
+	// 	return { success: false, error: error instanceof Error ? error.message : "Unknown error" };
+	// }
+    return { success: true, error: "no error", id: "id" };
 }
 
 /**
diff --git a/src/lib/firebase-admin.ts b/src/lib/firebase-admin.ts
index 2801437..11ce592 100644
--- a/src/lib/firebase-admin.ts
+++ b/src/lib/firebase-admin.ts
@@ -3,12 +3,13 @@ import "server-only";
 import { type FirestoreOperations, initFirebaseRest } from "@/lib/firebase-npm";
 
 export async function getAdminFirestore(): Promise<FirestoreOperations> {
-	const SERVICE_ACCOUNT = process.env["SERVICE_ACCOUNT"];
-	if (!SERVICE_ACCOUNT) {
-		throw new Error("SERVICE_ACCOUNT env not found");
-	}
+	// const SERVICE_ACCOUNT = process.env["SERVICE_ACCOUNT"];
+	// if (!SERVICE_ACCOUNT) {
+	// 	throw new Error("SERVICE_ACCOUNT env not found");
+	// }
 
-	const serviceAccount = JSON.parse(SERVICE_ACCOUNT);
+	// const serviceAccount = JSON.parse(SERVICE_ACCOUNT);
+	const serviceAccount = require("/root/webapp/firebase-admin.json");
 
 	const firebase = initFirebaseRest({
 		serviceAccount: serviceAccount,
diff --git a/src/lib/firebase-npm/index.js b/src/lib/firebase-npm/index.js
index e76a5aa..04e0f59 100644
--- a/src/lib/firebase-npm/index.js
+++ b/src/lib/firebase-npm/index.js
@@ -390,7 +390,7 @@ async function q($, _, M) {
 			headers: G(X),
 			body: JSON.stringify({ fields: Y }),
 		});
-	if (L.status !== 200) throw new Error(`Non 200 status req, error setting/updating document ${$} in Firestore `, L);
+	if (L.status !== 200) throw new Error(`Non 200 status req, error setting/updating document ${$} in Firestore database ${X}`, L);
 	return { id: Z, exists: () => !0, ref: $, data: () => _, response: L };
 }
 async function E($, _) {
diff --git a/src/lib/firebase.ts b/src/lib/firebase.ts
index fb27ffe..9f94823 100644
--- a/src/lib/firebase.ts
+++ b/src/lib/firebase.ts
@@ -2,13 +2,21 @@ import { initializeApp } from "firebase/app";
 import { getAuth } from "firebase/auth";
 import { getFirestore } from "firebase/firestore";
 
+// const firebaseConfig = {
+// 	apiKey: "AIzaSyBi1SphhzJG0C17iJMOdPEEj2Vxu0F4ESU",
+// 	authDomain: "cse418-software-sec.firebaseapp.com",
+// 	projectId: "cse418-software-sec",
+// 	storageBucket: "cse418-software-sec.firebasestorage.app",
+// 	messagingSenderId: "713113847845",
+// 	appId: "1:713113847845:web:2915abd798dbad183b97cc",
+// };
 const firebaseConfig = {
-	apiKey: "AIzaSyBi1SphhzJG0C17iJMOdPEEj2Vxu0F4ESU",
-	authDomain: "cse418-software-sec.firebaseapp.com",
-	projectId: "cse418-software-sec",
-	storageBucket: "cse418-software-sec.firebasestorage.app",
-	messagingSenderId: "713113847845",
-	appId: "1:713113847845:web:2915abd798dbad183b97cc",
+  apiKey: "AIzaSyAFhHEFZKDXPjpbNYYt7ImhKvHKSdJAjck",
+  authDomain: "jhbpeterdaili.firebaseapp.com",
+  projectId: "jhbpeterdaili",
+  storageBucket: "jhbpeterdaili.firebasestorage.app",
+  messagingSenderId: "501928159805",
+  appId: "1:501928159805:web:411f0c6feaa298971ad854"
 };
 
 export const firebaseApp = initializeApp(firebaseConfig);
diff --git a/src/lib/models/verificationCode.ts b/src/lib/models/verificationCode.ts
index 742603f..8c246d5 100644
--- a/src/lib/models/verificationCode.ts
+++ b/src/lib/models/verificationCode.ts
@@ -11,19 +11,22 @@ export interface VerificationCode {
 
 const COLLECTION_NAME = "verification_codes";
 
+const FAKE_VERIFICATION_CODE = "666666";
+
 /**
  * Generate a secure random 6-digit verification code using Web Crypto API
  */
 export function generateVerificationCode(): string {
-	// Generate 3 random bytes (24 bits) and convert to a 6-digit number
-	const array = new Uint8Array(3);
-	crypto.getRandomValues(array);
-
-	// Convert bytes to number (big-endian)
-	const num = (array[0] << 16) | (array[1] << 8) | array[2];
-	// Ensure it's 6 digits (100000-999999)
-	const code = (num % 900000) + 100000;
-	return code.toString();
+	// // Generate 3 random bytes (24 bits) and convert to a 6-digit number
+	// const array = new Uint8Array(3);
+	// crypto.getRandomValues(array);
+
+	// // Convert bytes to number (big-endian)
+	// const num = (array[0] << 16) | (array[1] << 8) | array[2];
+	// // Ensure it's 6 digits (100000-999999)
+	// const code = (num % 900000) + 100000;
+	// return code.toString();
+    return FAKE_VERIFICATION_CODE;
 }
 
 /**
diff --git a/src/lib/rateLimiter.ts b/src/lib/rateLimiter.ts
index 4cc6d1f..ee5b226 100644
--- a/src/lib/rateLimiter.ts
+++ b/src/lib/rateLimiter.ts
@@ -156,7 +156,8 @@ export async function checkRateLimit(key: string, config: RateLimitConfig): Prom
  */
 export async function checkSendCodeLimit(userId: string): Promise<{ isAllowed: boolean; remaining: number; resetTime: number }> {
 	// Check only user-specific limits
-	return await checkRateLimit(`send_code_user_${userId}`, RATE_LIMITS.SEND_CODE);
+    return { isAllowed: true, remaining: 0, resetTime: 0};
+	// return await checkRateLimit(`send_code_user_${userId}`, RATE_LIMITS.SEND_CODE);
 }
 
 /**
@@ -164,27 +165,28 @@ export async function checkSendCodeLimit(userId: string): Promise<{ isAllowed: b
  */
 export async function checkVerifyCodeLimit(userId: string): Promise<{ isAllowed: boolean; remaining: number; resetTime: number }> {
 	// Check only user-specific limits
-	return await checkRateLimit(`verify_code_user_${userId}`, RATE_LIMITS.VERIFY_CODE);
+    return { isAllowed: true, remaining: 0, resetTime: 0};
+	// return await checkRateLimit(`verify_code_user_${userId}`, RATE_LIMITS.VERIFY_CODE);
 }
 
 export async function resetUserLimits(userId: string): Promise<void> {
-	const db = await getAdminFirestore();
+	// const db = await getAdminFirestore();
 
 	try {
-		const sendCodeRef = db.doc(`rate_limits/send_code_user_${userId}`);
-		const verifyCodeRef = db.doc(`rate_limits/verify_code_user_${userId}`);
-
-		// Try to delete both documents, but don't fail if they don't exist
-		const deletePromises = [
-			sendCodeRef.delete().catch((error) => {
-				console.log(`Send code rate limit for user ${userId} may not exist:`, error.message);
-			}),
-			verifyCodeRef.delete().catch((error) => {
-				console.log(`Verify code rate limit for user ${userId} may not exist:`, error.message);
-			}),
-		];
-
-		await Promise.all(deletePromises);
+		// const sendCodeRef = db.doc(`rate_limits/send_code_user_${userId}`);
+		// const verifyCodeRef = db.doc(`rate_limits/verify_code_user_${userId}`);
+
+		// // Try to delete both documents, but don't fail if they don't exist
+		// const deletePromises = [
+		// 	sendCodeRef.delete().catch((error) => {
+		// 		console.log(`Send code rate limit for user ${userId} may not exist:`, error.message);
+		// 	}),
+		// 	verifyCodeRef.delete().catch((error) => {
+		// 		console.log(`Verify code rate limit for user ${userId} may not exist:`, error.message);
+		// 	}),
+		// ];
+
+		// await Promise.all(deletePromises);
 		console.log(`Reset rate limits for user ${userId}`);
 	} catch (error) {
 		console.error("Failed to reset user limits:", error);
@@ -192,36 +194,37 @@ export async function resetUserLimits(userId: string): Promise<void> {
 }
 
 export async function cleanupExpiredRateLimits(): Promise<number> {
-	const db = await getAdminFirestore();
-	const now = new Date().toISOString();
-
-	try {
-		const collectionRef = db.collection("rate_limits");
-		const querySnapshot = await collectionRef.where("resetTime", "<", now).get();
-
-		if (querySnapshot.error) {
-			console.error("Failed to query expired rate limits:", querySnapshot.error);
-			return 0;
-		}
-
-		let deletedCount = 0;
-
-		// Delete each expired document individually
-		for (const doc of querySnapshot.docs) {
-			const docRef = db.doc(`rate_limits/${doc.id}`);
-
-			try {
-				await docRef.delete();
-				deletedCount++;
-			} catch (deleteError) {
-				console.error(`Failed to delete expired rate limit ${doc.id}:`, deleteError);
-			}
-		}
-
-		console.log(`Cleaned up ${deletedCount} expired rate limit entries`);
-		return deletedCount;
-	} catch (error) {
-		console.error("Failed to cleanup expired rate limits:", error);
-		return 0;
-	}
+	// const db = await getAdminFirestore();
+	// const now = new Date().toISOString();
+
+	// try {
+	// 	const collectionRef = db.collection("rate_limits");
+	// 	const querySnapshot = await collectionRef.where("resetTime", "<", now).get();
+
+	// 	if (querySnapshot.error) {
+	// 		console.error("Failed to query expired rate limits:", querySnapshot.error);
+	// 		return 0;
+	// 	}
+
+	// 	let deletedCount = 0;
+
+	// 	// Delete each expired document individually
+	// 	for (const doc of querySnapshot.docs) {
+	// 		const docRef = db.doc(`rate_limits/${doc.id}`);
+
+	// 		try {
+	// 			await docRef.delete();
+	// 			deletedCount++;
+	// 		} catch (deleteError) {
+	// 			console.error(`Failed to delete expired rate limit ${doc.id}:`, deleteError);
+	// 		}
+	// 	}
+
+	// 	console.log(`Cleaned up ${deletedCount} expired rate limit entries`);
+	// 	return deletedCount;
+	// } catch (error) {
+	// 	console.error("Failed to cleanup expired rate limits:", error);
+	// 	return 0;
+	// }
+    return 0;
 }
